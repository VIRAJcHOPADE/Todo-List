<html>
  <head>
    <title>
      <%= title %>
    </title>
    
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.7.1/css/all.css"
      integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/home.css"/>
  </head>
  <body>
    <!-- container flex -->
    <div id="container">
      <!-- div for creating task -->
      <div>
        <h1 id="page-heading">TODO APP</h1>

        <!-- form for creating task -->
        <form id="task-input" action="/create-task" method="POST">
          <label for="description">TASK</label><br />

          <!-- enter the description -->
          <input
            type="text"
            name="description"
            id="description"
            placeholder="What do you need to do?"
            required
          />
          <div class="horizontal-divider"></div>

          <!-- select category -->
          <label for="category">CATEGORY</label><br />
          <select name="category" id="category">
            <option  value="" selected>Choose a category</option>
            <option class="chooses" value="personal">Personal</option>
            <option class="chooses" value="work">Work</option>
            <option class="chooses" value="school">School</option>
            <option class="chooses" value="cleaning">Cleaning</option>
            <option class="chooses" value="other">Other</option>
          </select>
          <div class="horizontal-divider"></div>

          <!-- enter the due date of the task -->
          <label for="date">DUE DATE</label><br />
          <input type="date" name="date" id="date" required /><br />
          <div class="horizontal-divider"></div>

          <button class="buto" type="submit" id="add-btn">
           <i class="fas fa-plus  pluses"></i>&nbsp; &nbsp;ADD TASK
          </button>
        </form>
      </div>

      <!-- div to show the tasks -->
      <div>
        <h3>&nbsp;</h3>
        <h1 id="page-heading">TASK LIST</h1>
        
        <!-- delete task form -->
        <form method="POST" action="/delete-task" id="delete-form">
          <!-- looping to show the tasks -->
         
          <div id="task-list-container">
            <% for (let i of task_list){%>
            <div class="horizontal-divider"></div>
            <div class="task">
              <!-- task name and checkbox -->
              <div class="checkbox ">
                <input type="checkbox" name="<%=i._id%>" /><%= i.task %>
              </div>
              
              <!-- task category -->
              <div class="category">
                <br>
                Category : <span><%= i.category %></span>
              </div>

              <!-- task date  -->
              <p><i class="fas fa-calendar-alt"></i> <%= i.date %></p>
            </div>
            <%}%>
          </div>

          <!-- delete task button -->
          <button type="submit" id="delete-btn">
           <i class="fas fa-trash"></i> DELETE TASK 
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
